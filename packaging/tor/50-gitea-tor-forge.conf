# mirror-agent and Gitea's own git-push subprocesses reach Tor via this
# loopback-only SOCKS port. This is Tor's default even unconfigured; it's
# pinned explicitly here so the stack doesn't silently break if the
# system torrc changes later.
SocksPort 127.0.0.1:9050
